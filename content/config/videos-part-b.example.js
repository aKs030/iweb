/*
  videos-part-b.example.js

  Example part file for the split-key approach. Copy to `/content/config/videos-part-b.js` and replace the placeholder string.
  Use Base64-encoded fragments (e.g. in browser console: `btoa('partB')`).

  Example (do not commit):
    export default 'REpM' // btoa('DLM')
*/

export default 'REPLACE_WITH_BASE64_PART_B'
