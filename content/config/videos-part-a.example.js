/*
  videos-part-a.example.js

  Example part file for the split-key approach. Copy to `/content/config/videos-part-a.js` and replace the placeholder string.
  Use Base64-encoded fragments (e.g. in browser console: `btoa('partA')`).

  Example (do not commit):
    export default 'QUJD' // btoa('ABC')
*/

export default 'REPLACE_WITH_BASE64_PART_A'
