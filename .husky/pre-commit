#!/bin/sh
# Husky pre-commit hook

echo "🔍 Running pre-commit checks..."

# Run lint-staged with auto-fix
npx lint-staged

EXIT_CODE=$?

if [ $EXIT_CODE -ne 0 ]; then
  echo ""
  echo "❌ Pre-commit checks failed!"
  echo "💡 Run: npm run fix"
  exit 1
fi

echo "✅ Pre-commit checks passed!"
