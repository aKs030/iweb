/*************************************************************************
 * For loading the footer.
 * The footer will not work if you open the file directly rather than on a web server.
 * NOTE: This script requires jQuery, which is not included in the project.
 * It seems unused or outdated based on the current footer implementation.
 *************************************************************************/

/*
(function () {
  "use strict";

  $(function () {
    // This assumes jQuery ($) is loaded, which it isn't.
    // Also, there might not be an element with class "footer-container".
    $(".footer-container").load("footer.html");
  });
})();
*/