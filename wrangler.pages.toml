# Cloudflare Pages Configuration
# https://developers.cloudflare.com/pages/configuration/

name = "iweb"
pages_build_output_dir = "."

[build]
command = "echo 'No build step required - serving static files directly'"
cwd = ""

[build.environment]
NODE_VERSION = "20"
NPM_VERSION = "10"
