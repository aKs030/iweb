name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ESLint
        run: npm run lint:check
      
      - name: Run Prettier
        run: npm run format:check
      
      - name: Check CSS stats
        run: npm run css:check

  build:
    name: Build & Bundle Analysis
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build project
        run: npm run build
      
      - name: Check bundle size
        run: |
          echo "📦 Bundle Size Analysis:"
          du -sh dist/
          du -sh dist/assets/*.js 2>/dev/null || echo "No JS bundles found"
          du -sh dist/assets/*.css 2>/dev/null || echo "No CSS bundles found"
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Run npm audit
        run: npm audit --audit-level=moderate
        continue-on-error: true
      
      - name: Check for outdated dependencies
        run: npm outdated || true

  workers-validation:
    name: Cloudflare Workers Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Install Wrangler
        run: npm install -g wrangler
      
      - name: Validate Worker syntax
        run: |
          echo "🔍 Validating AI Search Proxy..."
          node -c workers/ai-search-proxy/index.js
          echo "✅ AI Search Proxy syntax OK"
          
          echo "🔍 Validating YouTube API Proxy..."
          node -c workers/youtube-api-proxy/index.js
          echo "✅ YouTube API Proxy syntax OK"
      
      - name: Check wrangler.toml
        run: |
          if [ -f wrangler.toml ]; then
            echo "✅ wrangler.toml found"
            cat wrangler.toml
          else
            echo "⚠️ wrangler.toml not found"
          fi

  type-check:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run TypeScript type check
        run: npx tsc --noEmit || echo "⚠️ Type check completed with warnings"

  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run Knip (unused code detection)
        run: |
          echo "🔍 Checking for unused dependencies, exports, and files..."
          npx knip --reporter json > knip-report.json || true
          npx knip || echo "⚠️ Knip found potential issues"
      
      - name: Check for duplicate code
        run: |
          echo "🔍 Checking for duplicate code..."
          npx jscpd content/ pages/ workers/ --min-lines 10 --min-tokens 50 --format "javascript" --reporters "console" || echo "✅ No significant duplicates found"
        continue-on-error: true
      
      - name: Analyze code complexity
        run: |
          echo "📊 Analyzing code complexity..."
          npx es6-plato -r -d complexity-report content/ pages/ workers/ || echo "⚠️ Complexity analysis completed"
        continue-on-error: true
      
      - name: Check for console.log statements
        run: |
          echo "🔍 Checking for console.log in production code..."
          ! grep -r "console\.log" content/ pages/ --include="*.js" --exclude-dir=node_modules || echo "⚠️ Found console.log statements"
        continue-on-error: true
      
      - name: Upload Knip report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: knip-report
          path: knip-report.json
          retention-days: 7

  dependency-analysis:
    name: Dependency Analysis
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Check for circular dependencies
        run: |
          echo "🔄 Checking for circular dependencies..."
          npx madge --circular --extensions js content/ pages/ workers/ || echo "✅ No circular dependencies found"
      
      - name: Visualize dependency tree
        run: |
          echo "🌳 Generating dependency tree..."
          npx madge --extensions js --image dependency-graph.svg content/main.js || true
        continue-on-error: true
      
      - name: Check bundle impact
        run: |
          echo "📦 Analyzing bundle impact..."
          npx cost-of-modules || echo "✅ Analysis complete"
        continue-on-error: true
      
      - name: Upload dependency graph
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dependency-graph
          path: dependency-graph.svg
          retention-days: 7
        continue-on-error: true
